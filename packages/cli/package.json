{
  "name": "@tribble/sdk-cli",
  "version": "0.1.0",
  "description": "Tribble Platform CLI - Upload documents and trigger workflows",
  "private": false,
  "type": "module",
  "bin": {
    "tribble": "dist/index.cjs"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --sourcemap --clean"
  },
  "dependencies": {
    "@tribble/sdk": "*"
  }
}
